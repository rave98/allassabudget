import 'package:flutter/material.dart';

class RecurringExpenses extends StatelessWidget {
  RecurringExpenses({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Recurring expenses page!"),);
  }
}
