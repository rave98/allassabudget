import 'package:flutter/material.dart';

class AddExpenseManually extends StatelessWidget {
  AddExpenseManually({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("AddExpenseManually page!"),);
  }
}
